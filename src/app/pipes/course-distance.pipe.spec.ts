import { CourseDistancePipe } from './course-distance.pipe';

describe('CourseDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
